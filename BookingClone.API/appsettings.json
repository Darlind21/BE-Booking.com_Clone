{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=BookingCloneDb;Trusted_Connection=True;"
  },
  "JwtConfig": {
    "TokenKey": "Super secret key that only the server knows, Secret secret secret key that only the server knows, SeCret key that only the server knows, Super Secret key that only the server knows, Secret key that only the server knows, Secret key that only the server knows, Super Super Super Super SuperSuper Super Super Super Super Super Super Secret key that only the server knows",
    "Lifetime" : 3600
  }
}
